import type { useStandings } from '@/app/useStandings';

// Convenience type to avoid repeating the shape.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export type ReturnTypeOfUseStandings = ReturnType<typeof useStandings>;
